#pragma once

// PS / Mag+ / Mag- button debounce + edge dispatch. Stub — real
// implementation pending. Will own: 10 ms debounce, edge events for
// ps_trigger publish and lens_position_cmd nudges.
inline void buttons_init() {}
inline void buttons_tick() {}
