#pragma once

// LED ring + NeoKey driver. Stub — real implementation pending.
// Will own: Adafruit_NeoPixel strip, joystick override, ring/NeoKey
// rendering per SystemMode (PS_CAPTURING, HOMING, IDLE...).
inline void led_init() {}
inline void led_tick() {}
