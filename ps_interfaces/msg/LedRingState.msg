# Ring illumination as currently realized on the hardware, published at
# LED_STATE_RATE_HZ (10 Hz). Mirrors LedRingCommand; the same wire-size
# warning applies.
std_msgs/ColorRGBA[<=72] colors

builtin_interfaces/Time stamp
