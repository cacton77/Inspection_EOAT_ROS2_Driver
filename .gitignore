# arduino-cli --export-binaries output. Regenerated by every compile, and large
# enough that a habitual `git add --all` would commit megabytes of it.
firmware/*/build/

# Python bytecode. A stray import during testing was enough to commit one.
__pycache__/
*.py[cod]
